# API Layer

Contains HTTP controllers, middleware, filters, resources, and API dependency injection setup.

Tenant resolution should happen in this layer before requests reach application use cases.

