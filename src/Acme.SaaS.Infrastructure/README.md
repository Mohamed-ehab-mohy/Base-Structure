# Infrastructure Layer

Contains persistence, repository implementations, tenant services, caching, integrations, and framework-specific services.

This layer implements contracts defined by the application layer.

